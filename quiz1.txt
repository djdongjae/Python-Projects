다음 함수의 용도를 설명하시오.
1. sqlite3 라이브러리 cursor 의 execute 함수 
2. sqlite3 라이브러리 cursor 의 fetchmany 함수 
3. tkinter 라이브러리 Button 의 bind 함수
4. tkinter 라이브러리 Button 의 grid 함수 
5. 문자열에 적용하는 strip 함수


1. SQL 쿼리를 직접 실행할 수 있게 하는 함수.
2. SELECT 쿼리 실행 이후 이에 대한 결과 행을 반환하는 함수.
3. 특정 버튼에 클릭 등의 이벤트가 발생했을 때 호출될 함수를 지정하는 용도의 함수.
4. 특정 버튼의 위치를 지정하는데 사용하는 함수. 행과 열을 매개변수로 위치를 지정하고, width 등의 매개변수를 통해 다른 속성 또한 적용 가능. 
5. 문자열 양 끝에 위치하는 공백이나 특정문자를 제거하는 데 사용.